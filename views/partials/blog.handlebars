<article class="border-4 border-black mb-6 min-h-[100px]">
   <header class="flex justify-between bg-zinc-950 text-green-400 px-8 py-4">
     <h2 class="text-2xl font-bold">
        <a href="/posts/{{id}}">{{title}}</a>
    </h2>
    <h5 class="">
        Posted by {{user.username}} on {{formatDate created_at}}
    </h5>
   </header>
    <p class="px-8 py-4 text-2xl">
        {{content}}
    </p>    
</article>